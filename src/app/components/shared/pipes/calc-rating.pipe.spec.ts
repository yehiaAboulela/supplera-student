import { CalcRatingPipe } from './calc-rating.pipe';

describe('CalcRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
